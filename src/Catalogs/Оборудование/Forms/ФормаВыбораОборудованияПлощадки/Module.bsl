
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    Если ЭтотОбъект.Параметры.Свойство("ПлощадкаЭксплуатации") Тогда
        ЭтотОбъект.Список.Параметры.УстановитьЗначениеПараметра("ПлощадкаЭксплуатации", ЭтотОбъект.Параметры.ПлощадкаЭксплуатации);
    Иначе
        ЭтотОбъект.Список.Параметры.УстановитьЗначениеПараметра("ПлощадкаЭксплуатации", Константы.ПлощадкаЭксплуатацииПоУмолчанию.Получить());
    КонецЕсли;
    
КонецПроцедуры
